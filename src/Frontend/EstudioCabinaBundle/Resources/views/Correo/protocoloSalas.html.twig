<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <style type="text/css">
          th{
            text-align: right;
            margin-right: 20px;
          }

          .solicitud{
            text-align: justify;
          }

          table{
            width: 600px;

          }

          table td{
            padding: 10px;
            border:1px solid gray;
          }

          table th{
            border:1px solid gray;
            background-color: #F8F8F8;
            padding-right: 10px;
            width: 50%;
          }
        </style>
    </head>
{% for p in sala %}
    <body>
      <div align="center">
      <div><h3>SOLICITUD DE PROTOCOLO {{ p.reservacion.estudioCabina }}</h3></div>
      <div>
          <h4>Usted ha realizado una petición al departamento de protocolo, las especificaciones son las siguientes:</h4>
        </div>
      <table cellspacing=0>
        <tr>
          <th>Pauta:</th>
           <td>{{ p.reservacion.pauta|capitalize }}</td> 
        </tr>
        <tr>
           <th>Observaci&oacute;n:</th>
           <td>{{ p.reservacion.observacion|capitalize }}</td>    
        </tr>
        <tr>
          <th>Fecha Estimada:</th>
          <td>{{ p.reservacion.fecha |date('d-m-Y')}}</td>
        </tr>
        <tr>
          <th>Hora Programada:</th>
          <td>Inicio: {{ p.reservacion.horaInicio|date('H:i:s')  }}   |   Fin: {{ p.reservacion.horaFin|date('H:i:s')  }}</td>
        </tr>
        <tr>
          <th>N&uacute;mero de personas que asistir&aacute;n:</th>
          <td class="solicitud">{{ p.nropersonas}}</td>
        </tr>
        <tr>
          <th>Refrigerio:</th>
          <td class="solicitud">{{ p.refrigerio}}</td>
        </tr>
        <tr>
          <th>Solicitante:</th>
          <td>{% for s in solicitante %}{{ s.primerApellido | capitalize }} {{ s.primerNombre | capitalize}}{% endfor %}</td>
        </tr>
        <tr>
          <th>Extensi&oacute;n:</th>
          <td class="solicitud">{% for s in solicitante %}{{ s.extension}}{% endfor %}</td>
        </tr>
        <tr>
          <th>Dependencia:</th>
          <td class="solicitud">{% for s in solicitante %}{{ s.nivelorganizacional}}{% endfor %}</td>
        </tr>
        <tr>
          <th>Quien Aprob&oacute;:</th>
          <td>{% for p in perfil %}{{ p.primerApellido | capitalize }} {{ p.primerNombre | capitalize}}{% endfor %}</td>
        </tr>
      </table>
      <div>
          <h4>Su solicitud será atendida y la respuesta se le hará llegar por esta misma via.</h4>
        </div>
      <br>
    </div>
  </body>
  {% endfor %}
</html>