{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Jornadalaboral</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dias</th>
                <td>{{ entity.dias }}</td>
            </tr>
            <tr>
                <th>Horadesde</th>
                <td>{{ entity.horadesde }}</td>
            </tr>
            <tr>
                <th>Horahasta</th>
                <td>{{ entity.horahasta }}</td>
            </tr>
            <tr>
                <th>Observacion</th>
                <td>{{ entity.observacion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('jornadalaboral') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('jornadalaboral_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
